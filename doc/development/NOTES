# open Batteries.System;;
# File.open_in;;
- : ?mode:Batteries.System.File.open_in_flag list ->
    ?perm:Batteries.System.File.permission -> string -> InnerIO.input
= <fun>
# Gzip.open_in;;
- : ?mode:Extlib.File.open_in_flag list ->
    ?perm:Extlib.File.permission -> string -> InnerIO.input
= <fun>
# 


Fix above?


When writing batteries code, you can refer to other batteries modules
directly, but take note that Extlib is packed, so you have to refer to
its modules as Extlib.Foo
